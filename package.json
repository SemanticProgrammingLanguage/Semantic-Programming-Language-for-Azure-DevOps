{
  "name": "semantic-azure-devops-hybrid-highlighting",
  "version": "1.0.3",
  "private": true,
  "description": "Hybrid Azure DevOps syntax highlighting and source renderer for Semantic Programming Language.",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist out",
    "build": "npm run clean && webpack --mode production",
    "package": "npm run build && tfx extension create --manifest-globs vss-extension.json --output-path ./out"
  },
  "dependencies": {
    "azure-devops-extension-api": "^4.234.0",
    "azure-devops-extension-sdk": "^3.1.2"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "rimraf": "^6.0.1",
    "tfx-cli": "^0.24.2",
    "ts-loader": "^9.5.2",
    "typescript": "^5.9.2",
    "webpack": "^5.111.0",
    "webpack-cli": "^6.0.1"
  }
}
